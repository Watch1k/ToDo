import React from 'react'
import { layoutMainStyles } from '../Main/MainStyles'

export const autocompleteSearchStyles: React.CSSProperties = {
  width: 300,
}

export const layoutSearchStyles: React.CSSProperties = {
  ...layoutMainStyles,
  display: 'flex',
  alignItems: 'center',
}
